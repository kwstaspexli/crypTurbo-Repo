import * as React from 'react';
import Button from '@mui/material/Button';
import Typography from '@mui/material/Typography';

function MUI_THEME() {
    return ( 
        <div>
        <Typography color="primary" variant="h3">CrypTurbo</Typography>
        <Typography color="secondary" variant="h3">CrypTurbo</Typography>
        <Typography  variant="h3">CrypTurbo</Typography>
        <Button > CrypTurbo </Button>
        </div> );
}

export default MUI_THEME;